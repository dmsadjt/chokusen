services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: changeme
      POSTGRES_DB: chokusen
    ports:
      - "5432:5432"
    volumes:
      - chokusen_pgdata:/var/lib/postgresql/data

volumes:
  chokusen_pgdata:
